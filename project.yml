name: Flowstate
options:
  bundleIdPrefix: com.keegan            # change to your own reverse-DNS if you prefer
  deploymentTarget:
    macOS: "14.0"                       # 14+ gives us MenuBarExtra + .onKeyPress
targets:
  Flowstate:
    type: application
    platform: macOS
    sources: [Sources]
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.keegan.flowstate
        MARKETING_VERSION: "0.1.5"
        GENERATE_INFOPLIST_FILE: true
        # Tells macOS this is a menu-bar accessory (no Dock icon):
        INFOPLIST_KEY_LSUIElement: true
        # Shown in the one-time "Flowstate wants to control Terminal" Automation prompt:
        INFOPLIST_KEY_NSAppleEventsUsageDescription: "Flowstate uses Terminal to launch and place the spotify_player music player."
        ENABLE_APP_SANDBOX: false       # we shell out to spotify_player, aerospace, osascript
