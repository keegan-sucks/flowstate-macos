# XcodeGen output — regenerate with `xcodegen generate`, never commit the churn.
*.xcodeproj

# Build products
.build/
DerivedData/
build/
*.app

# macOS
.DS_Store

# Spotify OAuth token cache from scripts/sync_liked_playlist.py (contains tokens)
.cache-flowstate-liked-sync
.cache-*

# Python bytecode
__pycache__/
*.pyc
